import 'package:flutter/cupertino.dart';

class LearnPage extends StatelessWidget {
  const LearnPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Learn Page");
  }
}
